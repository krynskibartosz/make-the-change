// Shared application constants
// This file centralizes values that are shared across the app
// and would normally come from a context or server-side config.

/**
 * The current project ID. In a multi-project app, this would come
 * from a route parameter or a React context.
 */
export const CURRENT_PROJECT_ID = 'project-1'
