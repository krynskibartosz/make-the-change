# Base URL for the Web Client API (apps/web-client)
# Example local dev: http://localhost:3001
EXPO_PUBLIC_API_BASE_URL=

# Supabase (mobile should only use public anon key)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# IMPORTANT: Never use SUPABASE_SERVICE_ROLE_KEY in mobile apps
# Service role keys should only be used in trusted server environments

# Server-only (do not use in Mobile; for docs parity)
# DATABASE_URL=
# JWT_SECRET=
