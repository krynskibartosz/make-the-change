import { FaqContent } from './faq-content'

export const metadata = {
  title: 'FAQ - Make the Change',
  description: 'Questions fréquentes sur la plateforme Make the Change.',
}

export default function FaqPage() {
  return <FaqContent />
}
