I have analyzed the codebase and confirmed the existence of duplicate components in `apps/web-client` (and `apps/web`) and `packages/core`.

**Plan:**

1.  **Consolidate DataList & DataCard in Core**:
    *   Update `packages/core/src/shared/ui/data-list.tsx` to ensure it exports `DataCard` and matches all features from the client apps.
    *   Add JSDoc documentation to `DataList`, `DataCard`, and `DataListItem` in `packages/core`.
    *   Add unit tests for `DataList` in `packages/core` (targeting >80% coverage).
    *   Delete `apps/web-client/src/components/ui/data-list.tsx` and `apps/apps/web/apps/web-v3/src/components/ui/data-list.tsx`.
    *   Update imports in `apps/web-client` and `apps/web` to use `@make-the-change/core/shared/ui/data-list` (or similar).

2.  **Standardize EmptyState**:
    *   Verify `packages/core/src/shared/ui/empty-state.tsx` supports the "dashed" variant used in `apps/web`. (Confirmed it does).
    *   Delete `apps/apps/web/apps/web-v3/src/components/ui/empty-state.tsx`.
    *   Update imports in `apps/web` to use `@make-the-change/core/shared/ui/empty-state`.

3.  **Remove Redundant Utilities**:
    *   Update usages of `apps/web-client/src/lib/utils.ts` in `apps/web-client` to use `@make-the-change/core/shared/utils`.
    *   Refactor `formatCurrency` to `formatPrice`, `formatPoints` to `formatPoints` (accepting standard behavior), and `formatDate`.
    *   Delete `apps/web-client/src/lib/utils.ts`.

I will proceed step-by-step, starting with the Core refactoring, then the deletions and import updates.