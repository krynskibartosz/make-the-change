Je vous propose le plan d'action suivant pour répondre aux deux objectifs :

### 1. Réparer les Challenges (Connexion DB)
Puisque la table n'existe pas, nous allons créer l'infrastructure complète pour la gamification.

1.  **Création du Schéma de Base de Données**
    *   Modifier `packages/core/src/shared/db/schema.ts` pour ajouter le schéma `gamification`.
    *   Créer la table `gamification.challenges` avec les champs : `id`, `slug`, `title`, `description`, `reward_points`, `type` (daily, monthly, season), `is_active`.
    *   Créer une table de liaison `gamification.user_challenges` pour suivre la progression (optionnel pour l'instant, mais recommandé pour l'avenir).
    *   Générer une migration SQL via Drizzle Kit (si l'outil est configuré) ou préparer le fichier SQL de migration.

2.  **Mise à jour de `web-client`**
    *   Modifier `apps/web-client/src/app/[locale]/challenges/page.tsx`.
    *   Remplacer le tableau statique `challenges` par un appel au client Supabase : `supabase.schema('gamification').from('challenges').select('*').eq('is_active', true)`.
    *   Gérer les états de chargement et d'erreur.

### 2. Unifier les Types (`web-client` -> `core`)
L'objectif est de supprimer les types redondants et d'utiliser la source de vérité.

1.  **Identifier les doublons**
    *   Cibler `apps/web-client/src/app/[locale]/projects/[slug]/project-client.tsx` où `Project` et `Species` sont redéfinis.
    *   Vérifier d'autres fichiers comme `product-detail-client.tsx`.

2.  **Refactoring**
    *   Importer les types depuis `@make-the-change/core/schema` (ex: `import type { Project, Species } from '@make-the-change/core/schema'`).
    *   Supprimer les interfaces locales.
    *   Adapter le code si les noms de propriétés diffèrent légèrement (ex: `habitat` string[] vs `habitat_i18n`).

### Étapes d'exécution
1.  **Core** : Ajouter le schéma `challenges` dans `schema.ts`.
2.  **Migration** : Créer le fichier SQL de migration pour créer la table.
3.  **Client** : Refactoriser `project-client.tsx` pour utiliser les types core.
4.  **Client** : Connecter la page `Challenges` à la nouvelle table.

Validez-vous ce plan ?