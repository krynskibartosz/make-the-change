Je vais implémenter le système de thèmes complet (multi-marques + mode sombre/clair) dans `apps/web-client` en répliquant l'architecture existante de `apps/web`.

Voici le plan détaillé :

### 1. Configuration des Styles
*   **Créer** `apps/web-client/src/styles/theme-definitions.css` : Je copierai toutes les définitions de variables CSS (couleurs pour les thèmes Océan, Forêt, Néon, etc.) depuis le projet `apps/web`.
*   **Mettre à jour** `apps/web-client/src/app/globals.css` : J'importerai ce nouveau fichier CSS pour rendre les thèmes disponibles globalement.

### 2. Logique de Gestion des Thèmes (Provider)
*   **Mettre à jour** `apps/web-client/src/components/theme-provider.tsx` :
    *   Ajout d'un contexte React `ThemeContext` pour gérer la "marque" (`Brand`) active.
    *   Implémentation de la persistance via `localStorage` (clé `app-brand`).
    *   Logique pour injecter l'attribut `data-theme` sur la balise `<html>`.
    *   Export du hook `useThemeBrand` pour permettre aux composants de changer de thème.

### 3. Interface Utilisateur (UI)
*   **Créer** `apps/web-client/src/components/theme/theme-selection.tsx` :
    *   Je créerai un nouveau composant riche permettant de choisir à la fois le mode (Clair/Sombre/Système) et la marque visuelle.
    *   Il affichera une grille avec les aperçus des couleurs pour chaque thème (comme dans l'admin).

### 4. Intégration
*   **Modifier** `apps/web-client/src/app/[locale]/(dashboard)/dashboard/settings/settings-client.tsx` :
    *   Je remplacerai l'actuel `ThemeToggle` simple par le nouveau composant `ThemeSelection` complet dans l'onglet "Apparence".

Cela donnera à l'application client exactement les mêmes capacités de personnalisation que l'application d'administration.