Je propose d'exécuter les 3 phases identifiées pour aligner le projet sur la vision "Next.js 2026".

### 📋 Phase 1 : Performance & Modernisation (Immédiat)
**Objectif :** Activer les nouvelles optimisations de Next.js 15/16 pour un gain de performance immédiat.
1.  **Activer le React Compiler** :
    - Ajouter `experimental: { reactCompiler: true }` dans `apps/web/next.config.js` et `apps/web-client/next.config.js`.
    - Vérifier que le build passe sans erreur.
2.  **Adopter `"use cache"`** :
    - Remplacer l'usage potentiel de cache manuel ou non optimisé par la directive native `"use cache"` là où c'est pertinent (ex: récupération de données statiques).
    - *Note* : L'analyse n'a pas montré d'usage de `unstable_cache`, donc je me concentrerai sur l'activation de la fonctionnalité pour les futurs développements.

### 🛡️ Phase 2 : Architecture "Strict DAL" (Assainissement)
**Objectif :** Supprimer l'accès direct à la base de données (`drizzle`) dans l'application client (`apps/web-client`) pour respecter l'architecture "Zero-Bundle" et "Supabase Only".
1.  **Refactoring `dashboard/page.tsx`** :
    - Remplacer les requêtes Drizzle (`db.select...`) vers `investments` par des appels Supabase Client (`supabase.from('investments')...`).
    - *Pré-requis* : Je vérifierai d'abord que les règles de sécurité RLS (Row Level Security) existent pour la table `investments`.
2.  **Refactoring `dashboard/profile/actions.ts`** :
    - Remplacer les mutations Drizzle (`db.update...`) par des mutations Supabase (`supabase.from('profiles').update...`).

### 🌐 Phase 3 : Standardisation Middleware (Futur/Structure)
**Objectif :** Préparer la migration vers une gestion de session unifiée et sécurisée.
1.  **Standardiser l'Admin (`apps/web`)** :
    - Créer un `middleware.ts` standard (actuellement absent) pour gérer la protection des routes et le rafraîchissement des sessions Supabase, aligné sur `web-client`.
    - Cela prépare le terrain pour le pattern `proxy.ts` futur.

---

### ❓ Questions de validation
1.  **Pour la Phase 2 (DAL)** : Confirmez-vous que la table `investments` a bien des politiques RLS actives ? Si non, je devrai peut-être les créer ou conserver Drizzle temporairement.
2.  **Pour la Phase 3 (Middleware)** : Souhaitez-vous que j'implémente le middleware pour l'Admin (`apps/web`) maintenant, ou préférez-vous garder l'implémentation actuelle si elle fonctionne ?
