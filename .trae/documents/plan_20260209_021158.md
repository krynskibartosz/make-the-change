J'ai restauré la configuration qui fonctionnait précédemment.

**Actions effectuées :**
1.  **Restauré `apps/web/proxy.ts`** : Le fichier est de retour à la racine de l'application (au lieu de `src/`), ce qui correspond à la configuration originale.
2.  **Rétabli la logique DB** : Puisque `proxy.ts` s'exécute sur Node.js (et non Edge), j'ai remis la connexion à la base de données et les vérifications de rôles complètes.
3.  **Corrigé le build** : J'ai simplement ajusté la syntaxe de l'export `config` (suppression de `String.raw`) qui causait l'erreur de compilation, sans toucher à la logique métier.

Le build passe maintenant avec succès (`✓ Compiled successfully`). Si le problème d'URL Supabase persiste, c'est qu'il manque le fichier `.env.local`, mais le code est revenu à son état fonctionnel "d'avant".

Êtes-vous prêt à tester ?